- [PWA AR without WebXR](ar-without-webxr.md) — objects at fixed world positions from bearing/distance; camera rotates via device orientation, not objects; works broadly, no WebXR needed.
- [Device orientation → camera quaternion](device-orientation-camera-quaternion.md) — rotate the AR camera from alpha/beta/gamma (yaw+pitch+roll), not just compass heading, or overlays feel stuck to the screen.
- [iOS compass permission gesture](ios-compass-permission.md) — DeviceOrientationEvent.requestPermission() must be called synchronously from a user-gesture handler, not after an await chain.
- [Procedural rotor blade orientation](procedural-rotor-orientation.md) — build fanned blades in the local XY plane with no extra tilt so their spin axis (Z) naturally faces the viewer after Object3D.lookAt.
- [Manual toggle vs derived mode coupling](manual-toggle-vs-derived-mode-coupling.md) — a manual on/off toggle silently gets overridden if a visual effect checks `toggle || presetMode === "x"` instead of the toggle alone.
- [iOS Safari audio speaker routing](ios-audio-speaker-routing.md) — route Web Audio via MediaStreamAudioDestinationNode + hidden `<audio>` element to force main-speaker (not earpiece) playback.
- [WebGL canvas photo capture](webgl-canvas-photo-capture.md) — avoid `preserveDrawingBuffer` (risks context loss → black overlay); capture toDataURL synchronously right after render() instead.
- [In-app browser permission prompts](inapp-browser-permissions.md) — Messenger/Instagram/TikTok webviews often auto-deny camera/GPS without ever prompting; detect UA and tell users to open in Safari/Chrome.
- [Chained permission requests lose user-gesture](user-gesture-permission-chaining.md) — awaiting one permission prompt before requesting the next silently loses activation, causing later prompts to be denied with no dialog shown.
- [Sandboxed test browsers lack WebGL/GPU](sandbox-webgl-unavailable.md) — screenshot tool and Playwright test browser both fail "Failed to initialize WebGL"; add a mapboxgl.supported() fallback and verify non-map flows via Playwright instead.
- [Postgres ORDER BY DESC and NULLs](postgres-order-by-desc-nulls.md) — DESC sorts put NULL first by default, silently burying real values behind null-populated tie rows; use explicit NULLS LAST.
- [Playwright accessibility snapshot false "[active]"](playwright-active-snapshot-false-positive.md) — a toggle flagged as "not updating" via ARIA `[active]` may just be `:active` mouse-down noise; re-verify with a class-attribute diff before treating as a real bug.
- [ArcGIS resultOffset pagination duplicates](arcgis-pagination-duplicates.md) — paging without `orderByFields` can return the same feature on two pages; add a stable order field and dedupe by external key before bulk upsert.
- [GPS jitter + smoothed metric consistency](gps-jitter-and-smoothed-metric-consistency.md) — freeze input on a movement threshold AND rolling-average/throttle output; route all consumers through one shared smoothed value so they never disagree.
- [dBA-driven audio gain decoupling](dba-driven-audio-gain-decoupling.md) — drive ambient sound volume from the same estimate function shown on screen, and prefer an explicit user toggle over an automatic sensor heuristic when both audio and a display number must change together.
- [vite-plugin-pwa workbox precache limit](vite-pwa-workbox-precache-limit.md) — apps with heavy bundles (mapbox-gl, three.js) exceed the default 2MB precache cap and fail the build; raise `maximumFileSizeToCacheInBytes` proactively.
- [Uniform dB shift doesn't change contributing-count](uniform-db-shift-contributing-count.md) — a "within N dB of loudest" test is invariant to a uniform attenuation; add an absolute audibility floor too.
- [TFJS + deeplab peer version pin](tfjs-deeplab-tfjs3-peer-pin.md) — `@tensorflow-models/deeplab` needs `@tensorflow/tfjs` pinned to 3.x, not the latest 4.x meta-package.
- [Sky heuristic & ML disable tuning](sky-heuristic-and-ml-disable-tuning.md) — brightness/texture heuristics false-positive on bright walls if downsampled too far; aggressive ML slow-thresholds permanently disable heavy models within seconds of cold-start.
- [Child-to-parent effect feedback loop](child-to-parent-effect-feedback-loop.md) — lifting child state to a parent callback loops infinitely if any effect dependency traces back to an unmemoized `data ?? []` prop.
- [Downwind bearing vs wind direction "from"](downwind-bearing-vs-wind-from.md) — counterintuitive: downwind-of-source ⇔ meteorological wind-from-degrees ≈ bearing FROM listener TO source, not the reverse.
- [PDF `download` attribute fails on iOS PWA](pdf-download-attribute-ios-pwa.md) — `<a download>` silently no-ops on iOS Safari/installed-PWA standalone mode; use plain `target="_blank"` so the OS share/save sheet works instead.
- [Deployment type is not code-configurable](replit-deployment-type-not-code-configurable.md) — autoscale/vm/scheduled is a Deployments-pane setting; build an in-process scheduler as a default and document the Scheduled Deployment alternative for autoscale prod.
- [Per-pixel occlusion via onBeforeCompile](per-pixel-occlusion-shader-injection.md) — mask only the covered part of a 3D mesh (not the whole object) by sampling a shared screen-space grid texture per-fragment in a shader, not per-object opacity toggling.
- [ML inference fallback reliability](ml-inference-fallback-reliability.md) — bound warm-up exemptions by attempt count not success; a `Promise.race` timeout doesn't cancel the underlying work, so stale results can stack up.
- [Sensor/permission watchdog timeout](sensor-watchdog-timeout-pattern.md) — camera/GPS APIs can hang forever with neither success nor error; always pair with an independent timeout + retry, not just try/catch.
- [Occlusion grid resolution vs object size](per-pixel-occlusion-shader-injection.md) — a grid cell coarser than an object's on-screen extent blends visible+occluded parts into one tint, even with correct shader math.
- [Full-bleed overlay div blocks sibling pointer events](fullbleed-overlay-pointer-events-block.md) — an absolutely-positioned `inset-0` label/wrapper div painted on top of an interactive SVG/canvas silently eats all pointer events unless it itself (not just its children) has `pointer-events-none`.
- [Full-screen overlay masks loading state](full-screen-overlay-masks-loading-state.md) — an env-quality overlay independent of the primary `ready` flag can cover a working feature's loading/error UI, causing misreported bugs.
- [Duplicate geolocation request contention](duplicate-geolocation-request-contention.md) — a gesture-preserving throwaway `getCurrentPosition()` must use low accuracy or it competes with the real `watchPosition()` for the GPS chip and can stall it.
- [Mount-time-gated interval never (re)starts](mount-time-gated-interval-never-starts.md) — an effect with `[]` deps that early-returns based on data available only at mount time permanently disables itself if that data isn't ready yet (e.g. before an async GPS fix arrives).
- [Dual heavy WebGL contexts freeze mobile browsers](dual-webgl-context-freeze.md) — running a second heavy WebGL consumer (e.g. TF.js) alongside an already-active Three.js canvas can hang the whole page on mobile Safari/Chrome; removing the second context beat tuning its safety thresholds.
- [Nationwide-zoom payload bloat](nationwide-zoom-payload-bloat.md) — a "load everything in bounds" query is fine zoomed-in but becomes multi-MB of unrendered detail (e.g. full polygons) at country zoom; gate heavy geometry fields behind a summary/full detail param keyed to zoom/bbox span.
- [dB-subtraction-through-clamp no-op](dba-clamp-near-floor-attenuation.md) — subtracting dB then re-clamping through the same floor/ceiling normalize can silently produce zero perceptible change; multiply the linear gain directly instead.
- [Threshold-freeze vs EMA for visual position](threshold-freeze-vs-ema-for-visual-position.md) — a hard movement-threshold freeze (good for gating a recompute) causes teleport-snapping if reused for continuously-rendered positions; use a time-based EMA instead.
- [Duplicate orientation event sources](duplicate-orientation-event-sources.md) — listening to both `deviceorientationabsolute` and `deviceorientation` without dedup feeds two conflicting headings into one filter, causing AR jitter; latch onto the first absolute source and ignore the rest.
- [Adaptive two-rate heading smoothing](adaptive-two-rate-heading-smoothing.md) — a single fixed-tau EMA can't both reject magnetometer noise and track real turns; interpolate tau by delta size, and gate rendering on a stability-settled flag, not a blind timer.
- [SVG debug overlay not in a11y tree](playwright-svg-debug-overlay-not-in-a11y-tree.md) — a Playwright test can report decorative SVG markers "missing" purely because they're absent from the accessibility snapshot; re-verify visually before trusting it.
- [AR object grounding via real trig](ar-turbine-grounding-trig.md) — a fixed vertical offset + heuristic scale falloff makes AR objects "float"; derive both base Y and scale from real height-angle/distance trig instead.
- [React onWheel passive preventDefault](react-onwheel-passive-preventdefault.md) — React's synthetic onWheel is passive by default; preventDefault() inside it just warns, use a native `wheel` listener with `{passive:false}` instead.
- [Verifying a physical calibration gesture](sensor-gesture-sector-sweep-gating.md) — detect a "move device in pattern X" instruction actually happened by bucketing raw (unsmoothed) sensor readings into sectors and requiring most to be visited, not a fixed timer; always cap with a max-wait fallback.
- [Per-render scoring crash + committed-snapshot debounce](per-render-scoring-crash-and-recompute-debounce.md) — expensive per-marker scoring on every pan/zoom re-render crashes; memoize on explicit inputs + debounce a "committed" snapshot the scorer reads.
- [Tap-menu vs drag-move gesture conflict](tap-menu-vs-drag-move-gesture-conflict.md) — drag-to-move markers fights pan/pinch/double-tap map gestures; replace with tap→menu→explicit move-mode→commit-on-next-tap instead.
- [replit.md size trimming without content loss](replitmd-trim-without-content-loss.md) — split verbose per-file/per-feature detail into artifact-local `docs/*.md` reference files, keep replit.md as a slim index with pointers; verify losslessness by diffing bullet fingerprints.
- [Probabilistic-union multi-source impact](probabilistic-union-multi-source-impact.md) — combine several sources' distance-decay effects on one target via `1-Π(1-fᵢ)` so overlap increases impact but never exceeds 100%.
- [Boundary polygon live-edit indirection](boundary-polygon-live-edit-indirection.md) — keep live-edit/persisted/debug-overlay polygon state separate; a getter-based scorer needs an explicit version-counter dependency to invalidate memoized results.
- [Overlay z-index vs persistent indicator](overlay-zindex-vs-persistent-indicator.md) — a full-screen status overlay can silently hide an always-should-be-visible arrow/badge unless its z-index is explicitly higher than the overlay's.
- [AR tracking freeze-vs-fade tiering](ar-tracking-freeze-vs-fade-tiering.md) — freeze position instantly on degraded fused-sensor tier, but only fade visibility after a sustained grace period; don't reuse one timer for both.
- [Heading turn-confirm gate large-jump escape](heading-turn-confirm-gate-large-jump-escape.md) — a multi-sample "confirm it's a real turn" noise gate adds lag to fast real turns too; add a higher single-sample threshold that skips the wait.
- [DOM-measurement effect dependency mismatch](effect-ref-measurement-dependency-mismatch.md) — a ref-measuring useEffect must depend on the exact flag gating that element's mount, or it locks in a stale fallback (e.g. 0) forever.
- [Stale camera matrixWorldInverse same-frame](stale-camera-matrixworldinverse.md) — Three.js camera-space/frustum reads before renderer.render() in the same frame see last frame's orientation unless you force updateMatrixWorld() first.
- [Occlusion shader discard vs dim](occlusion-discard-vs-dim.md) — a per-pixel occlusion `discard` driven by a coarse sky classifier can hide an entire scene at once when the classifier misfires on a whole frame; only ever dim, never discard.
- [Safety-fallback vs hide-priority inversion](safety-fallback-vs-hide-priority-inversion.md) — a force-visible safety net is worthless unless checked FIRST in every parallel place opacity/visibility is computed, not just one.
- [Accumulated angle rotation vs clamped cone](accumulated-angle-rotation-vs-clamped-cone.md) — a "stuck" directional arrow is often a rendering bug (toggled fixed positions, clamped rotation cone), not a sensor bug; use one fixed anchor + unwrapped accumulated rotation.
- [Ref-driven audio EMA loop decoupled from React render rate](audio-ema-loop-decoupled-from-render-rate.md) — a volume that must update ≥10x/sec should EMA-smooth inside its own setInterval reading a ref, not depend on how often the caller's React state/effect re-renders.
- [Stale-but-good metric masks sensor silence](stale-good-metric-masks-sensor-silence.md) — a stability metric mutated only inside an event handler freezes at its last good value on total event silence; add an independent watchdog to detect silence itself.
- [Composite display metric vs safety gate](composite-quality-metric-vs-safety-gate.md) — add a separate weighted-average composite for a "feels misleading" display %, don't widen the narrow min() formula driving a safety gate.
- [Force-visible bypass for render gating](forcevisible-render-gate-bypass.md) — check a separate force-visible flag before applying confidence-based opacity, don't fold it into the same gate signal.
- [Loading overlay bleed-through — z-index, not shared gate](decoupled-readiness-timeline-bleedthrough.md) — never gate a scene's core render visibility on a loading screen's own closure; fix bleed-through with z-index + a fast/skippable overlay instead.
- [Early-mount WebGL guard](early-mount-webgl-guard.md) — mounting a `THREE.WebGLRenderer` component earlier/eagerly removes the implicit safety net of downstream gating; wrap renderer creation in its own try/catch.
- [Compressor flattens distance perception](audio-compressor-flattens-distance-perception.md) — a hard compressor + big makeup gain can squash a correctly distance-driven gain so "louder when closer" stops being audible.
- [Touchmove-triggered tile recompute hang](touchmove-triggered-tile-recompute-hang.md) — raw touchmove/pointermove firing setState directly re-triggers tile-layout recompute faster than the screen refreshes; throttle to one update per rAF.
- [Effect-before-early-return for lifted boolean state](effect-before-early-return-lifted-state.md) — a child reporting a derived boolean to its parent via callback must run that effect BEFORE any early `return null`, or the "false" report never fires.
- [Orientation values stuck despite live events](orientation-stuck-values-despite-live-events.md) — total-silence watchdog + "value stuck while correlated axis moves" fallback both miss ALL axes freezing identically while events keep firing; needs a third independent no-raw-change-despite-events check.
